# 告警与通知

所有告警规则均在管理后台配置，通过 Telegram 推送，消息模板可自定义。

## 资源阈值告警

为 CPU / 内存 / 磁盘 / 上行 / 下行设置阈值，可选均值或持续超限两种判定方式，并可设置持续时长，避免瞬时抖动误报。

## 离线告警

节点失联时推送通知。Agent 主动出站连接服务端，若节点频繁离线，请确认其能出站访问面板地址并核对 Token，见[安装 Agent](/guide/agent)。

## 到期与流量告警

节点可设置计费周期、价格、到期时间、流量额度与重置日，面板每日记录汇率快照用于多币种价格折算；临近到期或流量超额时触发告警。
