# 监控口径与采样

## 采样与上报

- CPU、内存和网速**每秒采样**，默认**每 3 秒压缩批量上传**。
- 卡片每秒显示一个真实采样值，正常连接下约 2～3 秒延迟；断线重连后跳过积压数据。
- 磁盘容量、GPU 等较慢指标单独缓存，历史数据按节点设置的保存间隔聚合。
- 增大实时上传间隔会降低卡片刷新频率。

## 内存口径

Linux 内存统计口径：

- 已用内存 = `MemTotal - MemFree - Cached - SReclaimable - Buffers + Shmem`
- Swap 已用量扣除 `SwapCached`

文件缓存不计入已用内存，共享内存计入；面板显示整机内存，而非 NodeFlare 进程占用。

## 历史数据

历史数据按节点的保存间隔聚合，默认保留 30 天，到期自动清理，见[数据库与备份](/guide/database)。
